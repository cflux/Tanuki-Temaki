# ============================================================================
# Tanuki Temaki - Centralized Environment Configuration
# ============================================================================
# This single .env file configures both backend and frontend packages.
# Copy this to .env and update with your actual values.
# ============================================================================

# Host Configuration - SINGLE SOURCE OF TRUTH
# ============================================================================
# Set this to your computer's hostname or IP address
# All URLs will be automatically derived from this setting
#
# For local development (desktop only):
HOST=localhost
#
# For mobile testing on same network:
# 1. Find your computer's IP: ipconfig (Windows) or ifconfig (Mac/Linux)
# 2. Set HOST to your IP address (e.g., 192.168.1.100)
# 3. Make sure your phone is on the same WiFi network
# Example: HOST=192.168.1.100
#
# For production deployment:
# Example: HOST=tanuki-temaki.com

# Database
# ============================================================================
DATABASE_URL=postgresql://tanuki:dev123@localhost:5432/tanuki_temaki
POSTGRES_PASSWORD=dev123

# Server Configuration
# ============================================================================
NODE_ENV=development
PORT=3000
FRONTEND_PORT=5173

# OAuth - Google (Get credentials from https://console.cloud.google.com/)
# ============================================================================
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# JWT Authentication
# ============================================================================
# Generate a secure random string for production
JWT_SECRET=your-secure-random-secret-key-change-this-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Admin Users
# ============================================================================
# Comma-separated list of usernames (case-insensitive)
ADMIN_USERNAMES=admin,your_username_here
